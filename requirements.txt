# Hawala v2 — Python dependencies
# Generated 2026-05-23 for cross-platform install (Mac + Windows)
#
# IMPORTANT (Windows): Do NOT run `pip install -r requirements.txt` directly.
# Use the wrapper: .\windows\install_deps.ps1
#
# Why: `growwapi` pins `numpy<2.0`. Anaconda Python 3.13 ships numpy 2.x.
# Pip's resolver tries to downgrade → source build → no MSVC → fail.
# The wrapper installs growwapi --no-deps FIRST, then this file resolves
# cleanly using Anaconda's existing numpy 2.x.
#
# growwapi is intentionally NOT in this file. The wrapper handles it.

beautifulsoup4>=4.12
fastapi>=0.110
feedparser>=6.0
jinja2>=3.1
lxml>=4.9
matplotlib>=3.8
numpy>=1.24
openpyxl>=3.1
pandas>=2.0
pillow>=10.0
plotly>=5.18
pyarrow>=14.0
pyotp>=2.9
pytz>=2024.1
pyyaml>=6.0
requests>=2.31
scikit-learn>=1.4
scipy>=1.11
seaborn>=0.13
statsmodels>=0.14
tqdm>=4.66
uvicorn>=0.27
watchdog>=4.0
websockets>=12.0
